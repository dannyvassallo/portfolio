<h1><%= @post.title %></h1>
<% if @post.image? %>
  <%= image_tag( @post.image.url ) %>
<% end %>
<p><%= @post.label %></p>
<p><%= @post.body %></p>
 <% if @user && @user.admin? %>
  <%= link_to "Edit", edit_post_path(@post), class: 'btn btn-success' %>
<% end %>